name: Build Linux server binary

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Python + libs)
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-dev libpython3-dev pkg-config libssl-dev

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python dependencies (firebase-admin)
        run: |
          python -m pip install --upgrade pip
          pip install firebase-admin

      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      # Ключевой момент: указываем PyO3, какой интерпретатор использовать
      - name: Build release binary
        env:
          PYO3_PYTHON: /usr/bin/python3
        run: cargo build --release

      - name: Copy Python helper
        run: cp fcm_helper.py target/release/

      - name: Upload binary artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-binary
          path: |
            target/release/start2          # замените на имя вашего бинарника
            target/release/fcm_helper.py